# LaTeX2e + BibLaTeX Typeset Boilerplate

Download and run `make all`. Output PDF of the typeset document will be in the
`output` directory.

### Assumptions
LaTeX2e is installed and configured on your local machine.
